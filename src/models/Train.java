package models;

public class Train extends Vehicle{
    public Train() {
    }

    public Train(String number, Integer seat, Integer speed, Integer weight, String color) {
        super(number, seat, speed, weight, color);
    }
}
